# Railway / Nixpacks: build and run the Node API only (frontend is on Vercel)
[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build:server"]

[start]
cmd = "node dist/server/index.js"
